<a class="editable"
   style="cursor:pointer;"
   data-name="{{ $name }}"
   data-type="{{ $type ?? 'text' }}"
   data-step="{{ $step ?? '' }}"
   data-emptytext="{{ $empty ?? '-' }}"
   data-pk="{{ $pk }}"
   data-url="{{ $url }}"
   data-value="{{ $value }}">
</a>
